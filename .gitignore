# Cargo build artifacts
/target

# Local release packages (mirrors what release-app.yml produces in CI)
/dist

# Binaries provisioned locally by the app (yt-dlp, ffmpeg, ffprobe, deno)
bin/

# Per-user runtime state generated by the app
mimirdlp.config
mimirdlp.queue
*.tmp
download_archive.txt
*.part
*.ytdl
